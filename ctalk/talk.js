function calculateCost() {
    let name = 'rupert'
    document.getElementById("name").value;
    let characterPrice = (name.length)
    document.getElementById("character-price").value;
    let cost = name.length * characterPrice;
    alert(`The cost to print "${name}" on a billboard is ${cost} dollars.`);
}

console.log(characterPrice * 25)